:root{
  --bg:#070A12;
  --panel:#0B1020;
  --panel2:#0A0F1C;
  --text:#EAF0FF;
  --muted:rgba(234,240,255,0.72);
  --line:rgba(255,255,255,0.10);

  --gold:#D4AF37;
  --neon:#6D5BFF;
  --neon2:#00E5FF;

  --radius:18px;
}

*{ box-sizing:border-box; }
html{ scroll-behavior:smooth; }
body{
  margin:0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Helvetica Neue", sans-serif;
  background:
    radial-gradient(900px 500px at 15% 10%, rgba(109,91,255,0.22), transparent 55%),
    radial-gradient(900px 500px at 85% 20%, rgba(0,229,255,0.18), transparent 55%),
    radial-gradient(1000px 700px at 50% 100%, rgba(212,175,55,0.10), transparent 55%),
    var(--bg);
  color:var(--text);
}

a{ color:inherit; text-decoration:none; }
.container{ width:min(1100px, calc(100% - 40px)); margin:0 auto; }

.muted{ color:var(--muted); }
.tiny{ font-size:12px; }
.mt-12{ margin-top:12px; }
.w-full{ width:100%; }

/* Topbar */
.topbar{
  position:sticky;
  top:0;
  z-index:50;
  backdrop-filter: blur(12px);
  background: rgba(7,10,18,0.65);
  border-bottom:1px solid var(--line);
}
.topbar-inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:14px 0;
}

.brand{
  display:flex;
  align-items:baseline;
  gap:10px;
  font-weight:800;
  letter-spacing:0.2px;
}
.brand-dot{
  width:10px;height:10px;border-radius:99px;
  background: linear-gradient(135deg, var(--neon), var(--neon2));
  box-shadow: 0 0 18px rgba(109,91,255,0.7), 0 0 18px rgba(0,229,255,0.55);
}
.brand-text{ font-size:18px; }
.brand-sub{
  font-size:12px;
  color:rgba(255,255,255,0.70);
  padding:4px 8px;
  border:1px solid var(--line);
  border-radius:999px;
}

.nav{
  display:flex;
  gap:18px;
  align-items:center;
}
.nav a{
  color:rgba(255,255,255,0.82);
  font-weight:600;
  padding:10px 10px;
  border-radius:12px;
}
.nav a:hover{ background: rgba(255,255,255,0.06); }

.nav-toggle{
  display:none;
  background:transparent;
  color:var(--text);
  border:1px solid var(--line);
  border-radius:12px;
  padding:10px 12px;
  cursor:pointer;
}

/* Mobile nav */
.mobile-nav{
  display:none;
  border-bottom:1px solid var(--line);
  background: rgba(7,10,18,0.92);
  backdrop-filter: blur(10px);
}
.mobile-nav a{
  display:block;
  padding:14px 20px;
  border-top:1px solid var(--line);
}

/* Buttons */
.btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  padding:12px 16px;
  border-radius:14px;
  border:1px solid var(--line);
  background: rgba(255,255,255,0.06);
  color:var(--text);
  cursor:pointer;
  font-weight:800;
  transition: transform .12s ease, box-shadow .12s ease, background .12s ease, border-color .12s ease;
  user-select:none;
}
.btn:hover{
  transform: translateY(-1px);
  background: rgba(255,255,255,0.09);
}
.btn:active{ transform: translateY(0px) scale(0.99); }

.btn-small{ padding:10px 12px; border-radius:12px; font-size:13px; }

.btn-primary{
  border-color: rgba(109,91,255,0.55);
  background: linear-gradient(135deg, rgba(109,91,255,0.95), rgba(0,229,255,0.70));
  box-shadow: 0 0 30px rgba(109,91,255,0.22), 0 0 30px rgba(0,229,255,0.15);
}
.btn-primary:hover{
  box-shadow: 0 0 38px rgba(109,91,255,0.28), 0 0 38px rgba(0,229,255,0.20);
}

.btn-ghost{
  background: rgba(255,255,255,0.05);
}

/* Hero */
.hero{ padding:44px 0 30px; }
.hero-grid{
  display:grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap:22px;
  align-items:start;
}
.badge{
  display:inline-flex;
  align-items:center;
  gap:10px;
  padding:10px 12px;
  border-radius:999px;
  border:1px solid var(--line);
  background: rgba(255,255,255,0.04);
  color: rgba(255,255,255,0.86);
  font-weight:700;
}
h1{
  margin:14px 0 10px;
  font-size: clamp(30px, 4vw, 52px);
  line-height:1.04;
}
.glow-text{
  text-shadow:
    0 0 22px rgba(109,91,255,0.35),
    0 0 22px rgba(0,229,255,0.20);
}
.hero-cta{ display:flex; gap:12px; margin:18px 0 10px; flex-wrap:wrap; }
.hero-stats{
  display:flex;
  gap:14px;
  margin-top:16px;
  flex-wrap:wrap;
}
.stat{
  border:1px solid var(--line);
  background: rgba(255,255,255,0.04);
  border-radius:16px;
  padding:12px 14px;
  min-width:140px;
}
.stat-num{ font-weight:900; font-size:18px; }
.stat-label{ color:var(--muted); font-size:12px; margin-top:4px; }

/* Cards */
.card{
  border:1px solid var(--line);
  background: rgba(11,16,32,0.70);
  border-radius: var(--radius);
  overflow:hidden;
  box-shadow: 0 16px 50px rgba(0,0,0,0.35);
}
.glow-card{
  position:relative;
}
.glow-card:before{
  content:"";
  position:absolute;
  inset:-2px;
  border-radius: calc(var(--radius) + 2px);
  background: linear-gradient(135deg, rgba(109,91,255,0.35), rgba(0,229,255,0.25), rgba(212,175,55,0.18));
  filter: blur(18px);
  opacity:0.9;
  z-index:-1;
}
.card-header{
  padding:18px 18px 0;
}
.card-header h3{ margin:0; }
.card-header p{ margin:8px 0 0; }
.card-body{ padding:18px; }

/* Form */
.field{ display:block; margin-top:12px; }
.field span{
  display:block;
  font-size:12px;
  color:rgba(255,255,255,0.72);
  margin-bottom:6px;
  font-weight:700;
}
input, select, textarea{
  width:100%;
  padding:12px 12px;
  border-radius:14px;
  border:1px solid rgba(255,255,255,0.12);
  background: rgba(0,0,0,0.20);
  color:var(--text);
  outline:none;
}
input:focus, select:focus, textarea:focus{
  border-color: rgba(0,229,255,0.55);
  box-shadow: 0 0 0 4px rgba(0,229,255,0.12);
}
.two{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:12px;
}
.check{
  margin-top:12px;
  display:flex;
  gap:10px;
  align-items:flex-start;
  color:rgba(255,255,255,0.82);
  font-size:13px;
}
.check input{ width:auto; margin-top:3px; }

.section{ padding:58px 0; }
.section-alt{
  background: linear-gradient(180deg, transparent, rgba(255,255,255,0.03), transparent);
  border-top:1px solid var(--line);
  border-bottom:1px solid var(--line);
}
.section-head{
  margin-bottom:20px;
}
.section-head h2{ margin:0 0 8px; font-size:28px; }
.section-head p{ margin:0; }

/* Plans */
.plans{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:14px;
}
.plan{
  position:relative;
  border:1px solid var(--line);
  background: rgba(255,255,255,0.035);
  border-radius: var(--radius);
  padding:18px;
  overflow:hidden;
}
.plan h3{ margin:0; }
.plan-top p{ margin:8px 0 0; }
.price{ display:flex; align-items:baseline; gap:6px; margin:14px 0 10px; }
.currency{ font-weight:900; }
.amount{ font-size:40px; font-weight:1000; letter-spacing:-1px; }
.period{ color:var(--muted); font-weight:700; }
.list{ margin:12px 0 16px; padding-left:18px; color:rgba(255,255,255,0.85); }
.list li{ margin:8px 0; }

.featured{
  border-color: rgba(109,91,255,0.55);
  background: linear-gradient(180deg, rgba(109,91,255,0.12), rgba(0,229,255,0.06));
  box-shadow: 0 0 36px rgba(109,91,255,0.10);
}
.ribbon{
  position:absolute;
  top:14px;
  right:-44px;
  transform: rotate(35deg);
  background: linear-gradient(135deg, rgba(212,175,55,0.95), rgba(109,91,255,0.90));
  color:#080A12;
  font-weight:1000;
  padding:8px 56px;
  border-radius:999px;
}

/* Features grid */
.grid-3{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:14px;
}
.feature{
  border:1px solid var(--line);
  background: rgba(255,255,255,0.035);
  border-radius: var(--radius);
  padding:18px;
}
.icon{ font-size:22px; }

.notice{
  margin-top:18px;
  padding:14px 16px;
  border-radius:16px;
  border:1px solid rgba(212,175,55,0.25);
  background: rgba(212,175,55,0.08);
  color: rgba(255,255,255,0.88);
}

/* FAQ */
.faq details{
  border:1px solid var(--line);
  background: rgba(255,255,255,0.03);
  border-radius: var(--radius);
  padding:14px 16px;
  margin:10px 0;
}
.faq summary{
  cursor:pointer;
  font-weight:900;
}
.faq p{ margin:10px 0 0; }

/* Order */
.order-grid{
  display:grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap:14px;
}
.contact{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
}
.contact-big{ font-weight:1000; font-size:18px; }
.divider{
  height:1px;
  background: var(--line);
  margin:18px 0;
}

.copybox{
  margin-top:14px;
  border:1px solid var(--line);
  border-radius:16px;
  background: rgba(0,0,0,0.18);
  overflow:hidden;
}
.copyhead{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;
  padding:12px 12px;
  border-bottom:1px solid var(--line);
}
.copytext{
  margin:0;
  padding:12px;
  white-space:pre-wrap;
  word-break:break-word;
  font-size:13px;
  color:rgba(255,255,255,0.88);
}

/* Footer */
.footer{
  padding:22px 0;
  border-top:1px solid var(--line);
  background: rgba(0,0,0,0.18);
}
.footer-inner{
  display:flex;
  justify-content:space-between;
  gap:12px;
  flex-wrap:wrap;
}

/* Responsive */
@media (max-width: 980px){
  .hero-grid{ grid-template-columns: 1fr; }
  .plans{ grid-template-columns: 1fr; }
  .grid-3{ grid-template-columns: 1fr; }
  .order-grid{ grid-template-columns: 1fr; }
  .nav{ display:none; }
  .nav-toggle{ display:inline-flex; }
  .mobile-nav{ display:block; }
}
